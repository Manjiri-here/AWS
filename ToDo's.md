1) Create an API to interact, manipulate (perform CRUD operations) data from S3 bucket. Without using UI how can an oject be stored and accessed and manipulated on S3.

2) Create a lambda to invoke a lambda time to time to avoid cold start. Check how its done in organization.
